---
const { eyebrow = 'Next step', title, body, buttonText, href } = Astro.props;
---
<section class="card">
  <p class="eyebrow">{eyebrow}</p>
  <h2>{title}</h2>
  <p class="muted">{body}</p>
  <a class="button" href={href}>{buttonText}</a>
</section>
